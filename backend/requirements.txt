fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
pyjwt>=2.9
# Required: Supabase signs session tokens with ES256, and PyJWT cannot verify
# elliptic-curve signatures without this. Without it every sign-in fails.
cryptography>=43.0
